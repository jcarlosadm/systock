<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Systock</title>
</head>
<body>

    <% if (locals.success) { %>
      <div>
        <p><%= success %></p>
      </div>
    <% } %>

    <div>
      Olá <%= loggedUser.name %>
    </div> 
    <a href="/logout">Logout</a>

</body>
</html>